<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marler.alumnus.mapper.MajorMapper">

    <!-- 查询所有专业 -->
    <select id="findAll" resultType="com.marler.alumnus.pojo.Major">
        SELECT id, college_id AS collegeId, major_name AS majorName, code, create_time AS createTime, update_time AS updateTime
        FROM major
        ORDER BY id ASC
    </select>

</mapper>
