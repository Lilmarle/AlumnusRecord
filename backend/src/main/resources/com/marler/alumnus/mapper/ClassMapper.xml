<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marler.alumnus.mapper.ClassMapper">

    <!-- 查询所有班级 -->
    <select id="findAll" resultType="com.marler.alumnus.pojo.Class">
        SELECT id, major_id AS majorId, class_name AS className, code, grade, create_time AS createTime, update_time AS updateTime
        FROM class
        ORDER BY id ASC
    </select>

</mapper>
